---
title: "2026-08-25"
type: journal
tags: [daily]
created: "2026-08-25"
updated: "2026-08-25"
status: active
summary: "FireSim_URP_PC 火焰面板/消防车面板批量改造 + Ignis 插件排坑；Day 30 知识问答 9/10（Day 29 两弱点攻破，对象池池空第 4 次栽跟头）"
---

# 2026-08-25 星期二

## Records

### 配电房/FireSim_URP_PC 项目 — 面板批量改造 + Ignis 排坑

- **FireSetPanel.cs 批量改造**：`flammableObject` 改属性返回 `allFlammableObjects[0]`；滑杆回调 `ApplyToAll(obj => ...)` 批量改所有 FlammableObject；`GetDisplayMax` 改纯面板配置；FlameOn/Off/Restart 批量调用
- **Ignis (OAVA-Flame) 排坑**：① `modifyFlamesOnRuntime` 默认 0(false)，运行时改字段不刷新 VFX，需手动开 true；② `isEffectOnForever` 只在 Start 读一次，运行时永久燃烧须手动设 `burnOutStart_s/burnOutLength_s = MaxValue`；③ 字段→VFX 同步是被动触发（`UpdateVFX()` 仅 `onFire==true` 且需 modifyFlamesOnRuntime）；④ 变体映射：`Simple` → `fireBoxGentleStylizedVariantPrefab`
- **FireTruckPanel 单例 + 懒加载**：初始失活物体 Awake 不执行 → Instance null NRE，需 `FindObjectOfType<FireTruckPanel>(true)` 懒加载 + Subscribe/Unsubscribe 判空

### 每日知识问答 — Day 30（9/10 ✅）

17:00 自动化出题 10 道（5 选择 + 5 简答）。选择题 4/5，简答题 5/5，总分 **9/10**。Day 29 两个失分点（链表反转三指针、朴素递归 O(2ⁿ)）选择题环节全攻破；两个新知识点先讲后答（DOTween 收尾、Frame Debugger）巩固全对。

- ✅ Q1 链表反转（[[链表反转]]）— 迭代需 3 指针 prev/cur/next：改指向前先存 next 防断链（Day 29 失分攻破）
- ❌ Q2 对象池 Get 池空（[[对象池实现]]）— **第 4 次选"扩容"**！正解=Instantiate 现造兜底（不入池，归还时入池/池满 Destroy）；口诀：取不到现造，放不下销毁，容量是天花板
- ✅ Q3 ContextMenu vs ContextMenuItem（[[Unity编辑器扩展开发入门]]）— 前者标记方法/右键组件菜单，后者标记字段/右键字段菜单（Day 18 后编辑器扩展回归首考）
- ✅ Q4 手动 GC.Collect（[[Boehm GC 保守式垃圾回收原理]]）— 同步 STW 卡顿 + 分代晋升反而更占内存；只回收托管堆
- ✅ Q5 朴素递归 Fibonacci 复杂度（[[动态规划入门]]）— O(2ⁿ) 指数分裂，空间 O(n) 递归栈深（Day 29 失分攻破）
- ✅ S1 DOTween Kill vs Complete（[[DOTween与协程选型]]，先讲后答）— Kill 停原地不触发 OnComplete；Complete 跳终点触发；场景销毁/覆盖/delay 期 Kill 也不触发
- ✅ S2 static event 泄漏 — 类型全局强引用方法 → 对象 Destroy 后无法 GC；实例事件随持有者一起回收；OnDestroy/OnDisable -= 反注册
- ✅ S3 中序迭代遍历（[[递归与迭代转换]]）— 显式栈：左链压到底→弹栈访问→转右子树；与前序"压栈即访问"的区别
- ✅ S4 Frame Debugger 合批排查（[[Unity 渲染批处理体系]]，先讲后答）— Window→Analysis→Frame Debugger 看 Break cause；同材质≠同实例（.material 自动克隆），改 sharedMaterial/MPB
- ✅ S5 点乘判断前后 — `Vector3.Dot(forward, toTarget)` >0 前方 <0 后方；叉乘 y 分量判断左右

下次抽查：⚠️ **对象池 Get 池空=Instantiate 现造**（Day 24/28/29/30 四次同错，最高优先级！已加记忆口诀）；其余 Day 29/30 失分点已清零

---

## Ideas

- **对象池池空反直觉点**：灯皇连续 4 次选"扩容"——根因可能是把"池容量"当成了集合可扩容语义。下次换"为什么控制峰值"的视角考，或让他在代码里写一遍 Get/Release 再答
- **红黑树路线继续**：08-21 已完 BST，下一步 AVL（平衡因子 + 四型旋转），最后红黑树
- **FireSim_URP_VR PC 版回退**：等设计稿期间抽离 UI 逻辑层（遗留）

## Tomorrow

- [ ] Day 30 弱点抽查：对象池 Get 池空=Instantiate 现造（第 5 次机会，必考！）
- [ ] C++ 红黑树路线：AVL 平衡因子 + 四型旋转（LL/RR/LR/RL）实现
- [ ] FireSim_URP_VR PC 版：等设计稿期间抽离 UI 逻辑层（遗留）

---
< [[2026-08-24]] | [[2026-08-26]] >
