---
title: "链表反转"
type: resource
tags: [算法, csharp, 面试, 数据结构]
created: "2026-07-31"
updated: "2026-07-31"
status: active
summary: "链表反转两种写法：三指针迭代法（O(n)/O(1)）与递归法（O(n)/O(n)），先保存 next 防断链"
---

# 链表反转

## 迭代法（三指针，面试首选）

```csharp
public ListNode ReverseList(ListNode head)
{
    ListNode prev = null;
    ListNode curr = head;
    while (curr != null)
    {
        ListNode next = curr.next;  // ① 先保存后继（防断链！）
        curr.next = prev;           // ② 反转指针：指向前一个
        prev = curr;                // ③ prev 前移
        curr = next;                // ④ curr 前移
    }
    return prev;                    // 原链表尾 = 新头
}
```

> ⚠️ **铁律**：① 必须先把 `next` 存下来！否则 `curr.next = prev` 一执行，后面的链表就"断"了，剩余节点全部丢失。

## 递归法（面试加分项）

```csharp
public ListNode ReverseList(ListNode head)
{
    if (head == null || head.next == null) return head;
    ListNode newHead = ReverseList(head.next);  // 递归反转后面的
    head.next.next = head;   // 让下一个节点指回自己
    head.next = null;        // 断开原方向
    return newHead;
}
```

## 复杂度对比（面试常问）

| | 时间复杂度 | 空间复杂度 |
|---|---|---|
| 迭代法 | O(n) | **O(1)** |
| 递归法 | O(n) | **O(n)**（递归栈深度） |

## 相关

- [[DFS与BFS遍历算法]]
