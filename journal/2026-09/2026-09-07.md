---
title: "2026-09-07"
type: journal
tags: [daily]
created: "2026-09-07"
updated: "2026-09-07"
status: active
summary: "FireSim_URP_PC 训练任务系统整套落地（SceneTaskBase 状态机 + 疏散/爆炸物转移实现 + TaskTrigger/HighlightMouse 交互链）；影院大门 E 键开门交互；16:19 批量新工具（NPC_Create/SceneLayoutLoader/NavMeshVisualizer）；任务数据层 CompletedTaskCount 隐患连续标注待修"
---

# 2026-09-07 星期一

## Records

### FireSim_URP_PC — 训练任务系统整套落地（9:34 批量 19 文件）

时间戳 9:34:53~55 同刻、创建=修改同刻，跨 LJW/ConducTraining + Lv/Task + Wt/TaskTips 三目录 → 整批同步/保存特征（同 09-03 先例判定：整批重建但内容有真演进）。对比上轮记录，任务类已从空壳占位填充为完整实现：

1. **SceneTaskBase 任务基类状态机**（Lv/Task/Core，此前为抽象骨架）
   - `TaskState` 五态：Locked → Available → InProgress → Completed / Failed；`IsCompleted/TaskStep/TaskProgress` 进度字段
   - 模板方法：`OnTaskStart/OnTaskSelect/OnTaskComplete/OnTaskFail/OnTaskReset` 五个虚点，`TaskStart()` 时 `MessageHandler.AddListener(Task_Train)` 订阅场景布置广播，收到 spawn 数据经 `GetTaskObjects` 按 `PlaceableCategory` 分类存入 `taskObjectDic`
   - `ShowHigh(go, isShow, isCollider, downAct, upAct)` 统一高光交互入口（委托给 CommUtils）

2. **TaskTrigger 触发器**（Control）——玩家进入触发高光、可 PushComplete/PushReset 切换激活：
```cs
private void OnTriggerEnter(Collider other)
{
    SetShowActive(true);
    if (IsSelect) return;             // 任务已选中则不再高亮
    CommUtils.ShowHigh(triggerParent, true, true);
}
public void PushComplete()  { SetShowActive(false); gameObject.SetActive(false); }
public void PushReset()     { SetShowActive(false); gameObject.SetActive(true); }
```

3. **任务实现类已填充完整**：
   - `PersonEvacuationTask`（人员被困疏散）：点击 NPC 点位 → 生成引导玩家（Player_NavMove 往返）→ `OnReturnComplete` 后 NPC 们 `SetLeader(player)` 跟随 → 各自 `isTargetEnd=true` 到达后计数 → 全员到齐 `TaskComplete()`
   - `ExplosiveTransferTask`（爆炸物转移）：点击拿取（置灰消失 + `placedCount++`）→ 首次拿取激活转移区 → 点击转移区 `SpawnAppend(placedCount)` 按 3 列 zPositions 排布新预制体 → `valuableList.Count >= spawnGameList.Count` 完成
   - `TaskEnum` 扩展：`WarningLineStair`、`PersonEscape`（人员自主逃生）等新任务类型
   - `CommUtils.ShowHigh`：outlineColor=黄 + `highlighted/enabled` 双开关 + 递归子 Collider 挂 `HighlightMouse` 注入 clickDown/UpAct + `col.enabled = isCollider`
   - `HighlightMouse`：OnMouseEnter（绿）/OnMouseExit（黄）/OnMouseDown/Up → 委托回调，HighlightPlus 高光（Profile 从 Resources 加载）

4. **ConducTraining 数据层补充**：`PlacedTrainObject`（包装 PlacedSceneObject 暴露 Category/PrefabId，用于"实训预制体"侧）、`TrainingTypes.cs`（TrainingTaskType 六类 + `ToTaskId/ToMockFileName` 映射 + PlaceableCategory 分类）

> 观察（未验证，待实测确认）：`SceneTaskBase.GetTaskObjects` 分类 switch 疑似复制粘贴笔误——`TrappedPerson/ValuableItem/Explosive` 三个 case 均写成 `taskObjectDic[TaskType.PersonInjured]`，应分别对应 PersonTrapped/ValuableTransfer/ExplosiveTransfer；`NPC_Create` 残留 `Debug.LogWarning("1 " + this.name + position)` 调试输出。见 Tomorrow。

### FireSim_URP_PC — 影院大门交互（YingYuanDaMen_Interaction，14:52 新增）

LJJ 影院场景入口交互（PC 鼠标适配版），挂 `InteractionObjectBase` 继承 E 键体系：
- `OnKeyboardDown()` → `OpenYingYuanDoor()`：左右门 `DOLocalRotate` 各转 ±120°（1s Linear）📎[[dotween详解]]
- `GuoDaoTrigger` 过道触发器（TriggerHandler 组件）进入时：隐藏 `BarrierObjectList` 障碍物 + 显示 `DianYingYuanObjectList` 影院物体 + `DianYingYuan` 列表物体 localPosition 复位
- `DoDianYingYuanPoint()`：开局把电影院整体移到 `localPosition.z = 1500` 藏到看不见处，过道触发再拉回来（同 HookRope 埋地藏物体的思路，只是换用远距离隐藏）

### FireSim_URP_PC — 16:19 新工具三件套（NPC_Create / SceneLayoutLoader / NavMeshVisualizer）

1. **NPC_Create**（Lv/Task/Control）——预设体子物体随机创建：
   - `TaskPrefabType` 六类型：Escape 单个 Idle / Rescue_Sit / Rescue_Down / Evacuation_1~3（1~3 名疏散 NPC）
   - 疏散 NPC 在半径 0.6m 圆周上分布：`GenerateRandomAngles` 保证两两夹角 ≥50° 防重叠；`Quaternion.LookRotation(-position)` 让 NPC z 轴朝向圆心；运行期 `AddComponent<NPC_NavFollower>` + BoxCollider（center 0.85/0.35×1.7×0.28 近似人形）

2. **SceneLayoutLoader**——按任务类型从 JSON 加载场景布置：
   - `LoadLayout(TrainingTaskType)` → `JsonFileHelper.TryReadJson<TaskData>(StreamingPath.MOCKAPI_FILES + ToMockFileName(taskType))`（StreamingAssets/MockApi 目录）
   - `SpawnSceneLayout` 遍历 `sceneLayout.placedObjects` → `FindPrefabById`（组训预制体数组匹配 PrefabId、返回同索引实训预制体）→ Instantiate 到对应 Parent（GL 高楼/DYY 影院/PDS 配电房）→ 按 PlaceableCategory 分组入 `spawnedObjectList` → `MessageHandler.Notify((int)MessageEventType.Task_Train, dict)` 广播给各 SceneTaskBase 订阅者
   - `ClearSpawnedObjects()` 倒序遍历 Destroy 清理（换房/重载布局用）

3. **NavMeshVisualizer**——NavMesh 可视化调试工具（导航盲区排查用）：
```cs
NavMeshTriangulation data = NavMesh.CalculateTriangulation(); // 世界坐标
mesh.vertices = data.vertices;  mesh.triangles = data.indices;
```
   - **坑**：`NavMesh.CalculateTriangulation` 顶点是**世界坐标**，显示物体必须 Awake 归零 `transform.position/rotation/localScale`，否则网格偏移到看不见
   - 材质：`Shader.Find` 三级回退（Unlit/Color → URP Unlit → Hidden/Internal-Colored）+ 手改混合状态（SrcAlpha/OneMinusSrcAlpha + ZWrite off + renderQueue=3000 透明），`RecalculateNormals/Bounds`
   - 阴影关：`ShadowCastingMode.Off` + receiveShadows=false

### FireSim_URP_PC — 任务数据层遗留（同批 9:34 刷新，风险再确认）

- `TaskStateManager.SetStatus`：`completedTaskCount++` 无幂等防重（重复 Finished 会翻倍）——**连续第 5 日（08-31~09-04 + 今日）标注未修**，今日文件虽在批量内但代码未见修复
- `TaskTipsPanel`：StepScrollView 上下步进 + Confirm 按钮选中项（todo 引导模块）

---
（今日其他三项目：配电房 / FireSim_URP / FireSim_URP_VR 均无 C# 改动；知识问答 automation memory 无 09-07 记录，最新 Day 35 09-04 → 无成绩单可写；conversation_search 09-05~07 无 Unity 会话 → 跳过）

## Ideas

- NavMeshVisualizer 思路可反哺 ConducTraining：训练前可视化导航网格排查"NPC 走不过/警戒线区域无 NavMesh"
- SceneLayoutLoader 的组训→实训同索引映射（FindPrefabById 返回 trainObjects[i]）设计较隐晦，后续可改成显式配对结构避免错位
- TaskTrigger 高亮链路（OnTriggerEnter + ShowHigh + IsSelect 三态）可抽象为通用"任务可交互物"组件模板

## Tomorrow

- [ ] SceneTaskBase.GetTaskObjects 分类复制粘贴 bug 实测确认并修正（TrappedPerson/ValuableItem/Explosive → 对应 TaskType）
- [ ] TaskStateManager CompletedTaskCount 防重修复（重复 Finished 只计一次）
- [ ] NPC_Create / SceneLayoutLoader 调试 LogWarning 清理（交付前）
- [ ] 观察今日 9:34 批量同刻保存是否复发（对比 08-19/09-03 先例），确认是否有外部同步机制

---
< [[2026-09-04]] | [[2026-09-08]] >
