---
title: "递归与迭代的转换"
type: resource
tags: [csharp, 算法, 面试, 数据结构]
created: "2026-08-04"
updated: "2026-08-04"
status: active
summary: "递归的栈溢出与性能开销、尾递归直接转循环、一般递归用栈/队列显式保存状态（DFS 用栈、BFS 用队列）、二叉树前序迭代模板"
---

# 递归与迭代的转换

## 两者的本质区别

| 维度 | 递归 | 迭代 |
|------|------|------|
| 机制 | 函数自调用，靠**调用栈**保存每层局部状态 | `while`/`for` 循环 + 显式变量 |
| 栈溢出 | 层数深了会溢出（Unity 主线程栈默认约 1MB） | 不会 |
| 开销 | 每次调用有压栈/出栈 | 无函数调用开销 |
| 代码 | 优雅、短 | 略啰嗦 |

## 转换套路（重点）

1. **尾递归 → 直接改循环**：尾递归本质就是循环。`sum(n) = n + sum(n-1)` 不是尾递归（有回溯），但 `sumAcc(n, acc)` 是——递归发生在最后一步，返回值直接透传。
2. **一般递归 → 看状态类型**：
   - 只关心"结果回溯"（阶乘）→ 用**临时变量累加**
   - 先深入再逐层返回处理（树的遍历）→ **自己用栈模拟调用栈**
   - 广度优先 → **队列**
3. **判断口诀**：递归的"状态"是什么，就用什么结构显式保存它——**深度优先用栈，广度优先用队列**。

## 经典案例

### 斐波那契
- 递归版：`F(n) = F(n-1) + F(n-2)`，重复计算爆炸，O(2ⁿ)
- 迭代版：两个变量滚动递推，O(n)，不会栈溢出

### 二叉树前序遍历（迭代版，高频）
```csharp
public IList<int> PreorderTraversal(TreeNode root)
{
    var result = new List<int>();
    if (root == null) return result;
    var stack = new Stack<TreeNode>();
    stack.Push(root);
    while (stack.Count > 0)
    {
        var node = stack.Pop();
        result.Add(node.val);          // 访问根
        if (node.right != null) stack.Push(node.right);  // 先压右
        if (node.left  != null) stack.Push(node.left);   // 后压左
    }
    return result;
}
```

> **为什么先压右再压左？** 栈是 LIFO，左孩子后进栈→先出栈，访问顺序就是"根→左→右"；压反了就成"根→右→左"了。

## 相关

- [[动态规划入门]]
- [[DFS与BFS遍历算法]]
- [[排序算法-快排归并堆排]]
