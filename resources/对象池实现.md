---
title: "对象池实现"
type: resource
tags: [unity, 性能优化, 对象池]
created: "2026-04-28"
updated: "2026-08-24"
status: active
summary: "Unity 对象池原理与 IObjectPool<T> 实现（踩坑修正版），含面试三连问：池空 Get=Instantiate 兜底 / 池满 Release=Destroy 截断 / 初始化放 OnEnable 原因"
source: ""
related: []
---

# 对象池实现

## 为什么需要对象池？

反复 `Instantiate` 和 `Destroy` 会：
- 产生 GC Alloc（垃圾回收压力）
- 分配和释放内存（CPU 开销大）
- 频繁触发 GC 导致卡顿

## 使用 IObjectPool<T>（Unity 2021+ 内置 API）

### 常见错误写法（踩坑）

```csharp
// ❌ 问题1：预创建的对象都是 active 的
createFunc: () => Instantiate(_prefab),

// ❌ 问题2：OnDisable + Invoke 导致循环调用
void OnDisable()
{
    Invoke(nameof(ReturnPool), 3f);
}
```

预创建时 Instantiate 出来的对象默认 active，而且 OnDisable 每次 SetActive(false) 都会触发，导致 Invoke 被反复调度，最终多次 Release 同一个对象。

### 正确写法

Pool 脚本：
```csharp
using UnityEngine;
using UnityEngine.Pool;

public class BulletPool : MonoBehaviour
{
    private IObjectPool<Bullet> _pool;
    [SerializeField] private Bullet _prefab;

    void Awake()
    {
        _pool = new ObjectPool<Bullet>(
            createFunc: () =>
            {
                var bullet = Instantiate(_prefab);
                bullet.gameObject.SetActive(false);   // 创建时就 inactive
                return bullet;
            },
            actionOnGet: bullet =>
            {
                bullet.gameObject.SetActive(true);
                bullet.StartReturnTimer(10f);          // 取出时启动倒计时
            },
            actionOnRelease: bullet =>
            {
                bullet.CancelReturnTimer();            // 回池时取消倒计时
                bullet.gameObject.SetActive(false);
            },
            actionOnDestroy: bullet => Destroy(bullet.gameObject),
            collectionCheck: true,                     // 开启重复回池检测
            defaultCapacity: 20,
            maxSize: 100
        );
    }

    public Bullet Get() => _pool.Get();
    public void Release(Bullet bullet) => _pool.Release(bullet);
}
```

Bullet 脚本：

```csharp
using UnityEngine;

public class Bullet : MonoBehaviour
{
    private BulletPool _pool;
    private float _returnTime;
    private bool _returnTimerActive;

    void Start()
    {
        _pool = FindObjectOfType<BulletPool>();
    }

    public void StartReturnTimer(float seconds)
    {
        _returnTime = seconds;
        _returnTimerActive = true;
    }

    public void CancelReturnTimer()
    {
        _returnTimerActive = false;
    }

    void LateUpdate()
    {
        if (!_returnTimerActive) return;

        _returnTime -= Time.deltaTime;
        if (_returnTime <= 0f)
        {
            _returnTimerActive = false;
            _pool.Release(this);
        }
    }
}
```

## 完整生命周期

```
游戏启动
  → 预创建 20 个对象，全部 SetActive(false)
  → Get()
    → SetActive(true) + 启动倒计时
    → 对象正常工作
  → 倒计时结束 → Release()
    → CancelTimer + SetActive(false)
    → 回到池中，等待下次 Get()
```

## 核心参数说明

| 参数 | 作用 |
|------|------|
| defaultCapacity | 预分配数量（初始 inactive） |
| maxSize | 最大上限，超出不再新建 |
| collectionCheck | 是否检查重复回池（开发阶段建议开启） |

## 面试三连问（Day 24/28/29 连续抽查点 ⚠️）

**Q1：为什么初始化放 OnEnable，不放 Start？**
- `Start` 只在对象**首次激活时执行一次**；复用对象每次 `SetActive(true)` 都会触发 `OnEnable`，但**不会再走 Start**
- 所以"每次复用都要重置的状态"必须放 OnEnable，配合 OnDisable 做释放/复位

**Q2：Get 时池子为空怎么办？**
- ❌ 不是"扩容"！池空 = 没有可用对象，"扩容（Capacity）"是集合容量概念，对池无意义
- ✅ 直接 `Instantiate` 实例化一个**全新对象兜底**，用完再归还进池
- 预热优化：启动时预创建 defaultCapacity 个（inactive）

> 🎯 **记忆口诀（Day 30 第 4 次栽跟头后强化）**：
> **取不到就现造，放不下就销毁，池容量是天花板不是地板。**
> 池空时现场 `Instantiate` 的实例**不入池**（归还时才入池，池满则 Destroy）；"扩容入池"会无限撑大 maxSize，失去**控制实例峰值**的意义——对象池是**有上限的周转站**，不是无限仓库。
> 形象比喻：自助餐餐台有固定餐位，菜没了现炒一盘端走（不入池），盘子堆不下了直接倒垃圾桶（Destroy），餐台永远不会无限扩建。

**Q3：Release 时池子满了怎么办？**
- ✅ 超过 `maxSize` 的直接 `Destroy`（截断），防止池无限膨胀
- `ObjectPool<T>` 内置该行为：超限对象走 `actionOnDestroy` 回调销毁

## ⚠️ 常见坑点

1. **不要在 OnDisable 里回池** — OnDisable 在 SetActive(false) 时触发，导致循环
2. **预创建的对象要先设 inactive** — 否则游戏一开始就有一堆 active 对象在场景里
3. **回池前取消计时器** — 避免倒计时回调时对象已被其他逻辑释放

## 相关笔记

- [[DOTS详解]] — 海量对象的另一种高性能方案
- [[Profiler自定义采样]] — 用 Profiler 对比有无对象池的 GC 差异
