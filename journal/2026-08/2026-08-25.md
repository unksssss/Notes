---
title: "2026-08-25"
type: journal
tags: [daily]
created: "2026-08-25"
updated: "2026-08-25"
status: active
summary: "FireSim_URP_PC NPC 角色/任务系统批量搭建 + 火焰面板批量改造 + Ignis 插件排坑；Day 30 知识问答 9/10（Day 29 两弱点攻破，对象池池空第 4 次栽跟头）"
---

# 2026-08-25 星期二

## Records

### FireSim_URP_PC — NPC 角色/任务系统批量搭建（09:48，19 文件）

`Assets/Scripts/Author/Lv/Task/` 批量新建 NPC 角色控制 + 场景任务 + 编辑器工具三组代码：

- **CharacterMoveController.cs** — NavMeshAgent 去程/返程移动状态机：`Init(start, target, returnMode)` 先 `NavMesh.SamplePosition` 吸附起终点，`PlaceAgentOnNavMesh` 用 `Warp` 放置（失败则 disable/enable 重建）；`MoveRoutine` 协程依次去程→`OnOutComplete`→返程→`OnReturnComplete`，整段路径固定速度（`autoBraking=false` + 每帧恢复 `agent.speed`）；到达判定 `remainingDistance <= stoppingDistance + arrivalTolerance`，路径状态机校验 `pathPending`/`PathComplete` + 移动超时兜底；失败统一走 `HandleMoveFailed` 回调 `OnMoveFailed`
  ```csharp
  // 位移全交 Agent，显示方向脚本控制（关键三配置）
  animator.applyRootMotion = false;
  agent.updateRotation = false;   // 手动 Slerp 转向（desiredVelocity 方向）
  agent.autoBraking = false;      // 关闭自动减速，整段匀速

  // 协程内部清理时必须传 false：不能 StopCoroutine 正在执行的自己
  private void StopAllMovement(bool stopCurrentCoroutine = true)
  { if (stopCurrentCoroutine && moveCoroutine != null) StopCoroutine(moveCoroutine); ... }
  ```
  返程双模式：`ReturnModeCarry`（isCarry 搬运动画 + 3f 速度）vs `ReturnModeDown`（isDown + 2f），用 `readonly struct ReturnModeConfig` 映射动画参数与速度；Animator 参数全部 `static readonly int` + `StringToHash` 缓存 [[Animator参数与性能优化]]
- **CharacterFollower.cs** — NPC 跟随：`SetLeader` + `refreshInterval=0.2s` 节流刷新目标点（避免每帧 SetDestination）；距离 > followDistance 才移动；`isTargetEnd` 到达自动 `StopFollow`；bool 动画只在状态变化时 `SetBool`（防每帧赋值）[[Animator参数与性能优化]]
- **CharacterNavPass.cs**（类名 NarrowPassDetector）— NPC 堵路自救：`SphereCast` 前方探测 NPC Layer（抬高 0.5m 防地面干扰）→ 停 2s 超时后 `NoObstacleAvoidance` 降速 0.8 挤过 → 再卡 1s 直接 `Warp` 随机侧移 0.2m 兜底；畅通时恢复 `HighQualityObstacleAvoidance`
- **TaskTrigger.cs** — 任务触发器：`OnTriggerEnter` 打开任务提示 `SetShowActive(true)` + `HighlightMouse.ShowHigh` 高亮联动；`PushComplete` 隐藏提示并 `SetActive(false)` 自身
- **12 个 SceneTask 场景任务**（ElectricalSafety/ExplosiveTransfer/FireElevator/FireSuppression/PersonEscape/PersonEvacuation/PersonRescue/ValuableTransfer/VirtualRealStep/WarningLine/WarningLineStair）：统一挂 `[ContextBanner("xx任务")]` 特性；`FireSuppressionTask` 已接入 `SceneTaskBase` 抽象基类（`GetTaskType()` 返回 `TaskType.FireSuppression` + 4 个虚方法空实现），其余多为空壳占位——骨架阶段
- **ContextBanner 编辑器工具**（Editor/ContextBannerEditor.cs + EditorSub/ContextBannerAttribute.cs）— 自定义类特性 + 全局 Inspector 横幅：`[CustomEditor(typeof(MonoBehaviour), true)]` 钩住所有组件，反射 `GetCustomAttribute<ContextBannerAttribute>` 后在 Inspector 顶部用 `GUIStyle("Box")` 居中白字显示任务横幅 [[Unity编辑器扩展开发入门]]
  ```csharp
  [CustomEditor(typeof(MonoBehaviour), true)]  // true=子类也生效，全局钩子
  [CanEditMultipleObjects]
  public class ContextBannerEditor : Editor {
      ContextBannerAttribute attr = mono.GetType().GetCustomAttribute<ContextBannerAttribute>();
      ... // 有横幅先画，再 DrawDefaultInspector()
  }
  ```
- **HighlightMouse.cs** — 外发光交互（HighlightPlus）：`OnMouseEnter` 绿 / `OnMouseExit` 黄描边 + `OnMouseDown/Up` 事件回调；`ShowHigh(go, isShow, isCollider)` 统一开关高亮与 Collider；Profile 用 ResManager 资源系统加载

### 配电房/FireSim_URP_PC 项目 — 面板批量改造 + Ignis 排坑

- **FireSetPanel.cs 批量改造**：`flammableObject` 改属性返回 `allFlammableObjects[0]`；滑杆回调 `ApplyToAll(obj => ...)` 批量改所有 FlammableObject；`GetDisplayMax` 改纯面板配置；FlameOn/Off/Restart 批量调用
- **Ignis (OAVA-Flame) 排坑**：① `modifyFlamesOnRuntime` 默认 0(false)，运行时改字段不刷新 VFX，需手动开 true；② `isEffectOnForever` 只在 Start 读一次，运行时永久燃烧须手动设 `burnOutStart_s/burnOutLength_s = MaxValue`；③ 字段→VFX 同步是被动触发（`UpdateVFX()` 仅 `onFire==true` 且需 modifyFlamesOnRuntime）；④ 变体映射：`Simple` → `fireBoxGentleStylizedVariantPrefab`
- **FireTruckPanel 单例 + 懒加载**：初始失活物体 Awake 不执行 → Instance null NRE，需 `FindObjectOfType<FireTruckPanel>(true)` 懒加载 + Subscribe/Unsubscribe 判空

### 每日知识问答 — Day 30（9/10 ✅）

17:00 自动化出题 10 道（5 选择 + 5 简答）。选择题 4/5，简答题 5/5，总分 **9/10**。Day 29 两个失分点（链表反转三指针、朴素递归 O(2ⁿ)）选择题环节全攻破；两个新知识点先讲后答（DOTween 收尾、Frame Debugger）巩固全对。

- ✅ Q1 链表反转（[[链表反转]]）— 迭代需 3 指针 prev/cur/next：改指向前先存 next 防断链（Day 29 失分攻破）
- ❌ Q2 对象池 Get 池空（[[对象池实现]]）— **第 4 次选"扩容"**！正解=Instantiate 现造兜底（不入池，归还时入池/池满 Destroy）；口诀：取不到现造，放不下销毁，容量是天花板
- ✅ Q3 ContextMenu vs ContextMenuItem（[[Unity编辑器扩展开发入门]]）— 前者标记方法/右键组件菜单，后者标记字段/右键字段菜单（Day 18 后编辑器扩展回归首考）
- ✅ Q4 手动 GC.Collect（[[Boehm GC 保守式垃圾回收原理]]）— 同步 STW 卡顿 + 分代晋升反而更占内存；只回收托管堆
- ✅ Q5 朴素递归 Fibonacci 复杂度（[[动态规划入门]]）— O(2ⁿ) 指数分裂，空间 O(n) 递归栈深（Day 29 失分攻破）
- ✅ S1 DOTween Kill vs Complete（[[DOTween与协程选型]]，先讲后答）— Kill 停原地不触发 OnComplete；Complete 跳终点触发；场景销毁/覆盖/delay 期 Kill 也不触发
- ✅ S2 static event 泄漏 — 类型全局强引用方法 → 对象 Destroy 后无法 GC；实例事件随持有者一起回收；OnDestroy/OnDisable -= 反注册
- ✅ S3 中序迭代遍历（[[递归与迭代转换]]）— 显式栈：左链压到底→弹栈访问→转右子树；与前序"压栈即访问"的区别
- ✅ S4 Frame Debugger 合批排查（[[Unity 渲染批处理体系]]，先讲后答）— Window→Analysis→Frame Debugger 看 Break cause；同材质≠同实例（.material 自动克隆），改 sharedMaterial/MPB
- ✅ S5 点乘判断前后 — `Vector3.Dot(forward, toTarget)` >0 前方 <0 后方；叉乘 y 分量判断左右

下次抽查：⚠️ **对象池 Get 池空=Instantiate 现造**（Day 24/28/29/30 四次同错，最高优先级！已加记忆口诀）；其余 Day 29/30 失分点已清零

---

## Ideas

- **对象池池空反直觉点**：灯皇连续 4 次选"扩容"——根因可能是把"池容量"当成了集合可扩容语义。下次换"为什么控制峰值"的视角考，或让他在代码里写一遍 Get/Release 再答
- **红黑树路线继续**：08-21 已完 BST，下一步 AVL（平衡因子 + 四型旋转），最后红黑树
- **FireSim_URP_VR PC 版回退**：等设计稿期间抽离 UI 逻辑层（遗留）

## Tomorrow

- [ ] Day 30 弱点抽查：对象池 Get 池空=Instantiate 现造（第 5 次机会，必考！）
- [ ] C++ 红黑树路线：AVL 平衡因子 + 四型旋转（LL/RR/LR/RL）实现
- [ ] FireSim_URP_VR PC 版：等设计稿期间抽离 UI 逻辑层（遗留）

---
< [[2026-08-24]] | [[2026-08-26]] >
